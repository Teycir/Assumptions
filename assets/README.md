# assets/

Place demo media here, e.g. `demo.gif` showing `/assumptions` run against
the `fixtures/duplicate-checkout` or refund example from
`examples/01-duplicate-payment.md`.

Suggested demo script:

1. Open a terminal in a repo containing the `duplicate-checkout` fixture.
2. Run `/assumptions`.
3. Let the ledger render, highlighting the P0 finding.

No binary media is checked in yet.
